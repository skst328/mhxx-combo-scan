# cargo のビルド成果物
/target

# wasm-pack の出力。`wasm-pack build --target web` で作り直す
/pkg

# 全コマぶんの ROI (36MB)。tools/dump_golden.py --all-frames で作り直す。
# git に入れるのは tests/fixtures の代表コマだけ
/tests/golden/frames
